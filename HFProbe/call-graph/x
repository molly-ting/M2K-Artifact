#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHONPATH="${SCRIPT_DIR}/py${PYTHONPATH:+:${PYTHONPATH}}" \
    python3 "${SCRIPT_DIR}/py/sxia/main.py" "$@"
